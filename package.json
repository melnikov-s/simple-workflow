{
  "name": "simple-workflow",
  "version": "1.0.0",
  "description": "Automated plan executor using Codex SDK with worker/reviewer loop",
  "type": "module",
  "scripts": {
    "start": "npx tsx run-plan.ts",
    "run-plan": "npx tsx run-plan.ts"
  },
  "dependencies": {
    "@openai/codex-sdk": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
