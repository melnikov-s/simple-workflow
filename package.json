{
  "name": "simple-workflow",
  "version": "1.0.0",
  "description": "Automated plan executor using OpenCode SDK with worker/reviewer loop",
  "type": "module",
  "scripts": {
    "start": "npx tsx run-plan.ts",
    "run-plan": "npx tsx run-plan.ts"
  },
  "dependencies": {
    "@opencode-ai/sdk": "^1.1.0",
    "effect": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
